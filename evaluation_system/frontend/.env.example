# Development-only Vite proxy target.
EVAL_API_DEV_PROXY=http://127.0.0.1:8090

# Production browser requests remain same-origin through Nginx. Do not put a
# production API hostname in a Vite build-time environment file.
